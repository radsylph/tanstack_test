import { useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import './App.css'

function App() {

  const query = useQuery({
  })

  return (
    <>
    
    </>
  )
}

export default App
